#include "Lua.h"

Lua::Lua(){

}

/*LuaFile Lua::OpenLua(string fileName) {
    LuaFile* L = luaL_newstate();
    luaL_dofile(L, fileName);
    luaL_openlibs(L);       
    lua_pcall(L, 0, 0, 0);
    return L;
}*/
